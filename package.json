{
  "name": "peezy-tech-identity",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.3.11",
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "@better-auth/core": "1.7.0-rc.1",
    "kysely": "0.29.4"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun --cwd packages/identity build && bun --cwd packages/server build && bun --cwd apps/server build",
    "dev": "bun --cwd apps/server dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "package:smoke": "bun scripts/package-smoke.ts",
    "test": "bun --cwd packages/identity build && bun --cwd packages/identity test && bun --cwd packages/server test && bun --cwd apps/server test",
    "typecheck": "bun --cwd packages/identity build && bun --cwd packages/identity typecheck && bun --cwd packages/server typecheck && bun --cwd apps/server typecheck && tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/bun": "1.3.11",
    "prettier": "3.6.2",
    "typescript": "5.9.3"
  }
}
